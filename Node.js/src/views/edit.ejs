<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UPDATE USER</title>
</head>
<link href="/css/style.css" rel="stylesheet">

<body>
    <%= JSON.stringify(userEdit) %>
        <div class="topnav">
            <a href="/">Home</a>
            <a href="/create">create user</a>
        </div>
        <div>
            <div class="form-add-new">
                <form action="/update-user" method="POST">
                    <fieldset>
                        <legend>UPDATE A USER </legend>

                        <div class="input-group d-none">
                            <label>Id : </label>
                            <input type="text" value="<%= userEdit.id %>" name="userId" />
                        </div>

                        <div class="input-group">
                            <label>Email:</label>
                            <input type="text" value="<%= userEdit.email %>" name="Email" />
                        </div>

                        <div class="input-group">
                            <label>Name:</label>
                            <input type="text" name="myName" value="<%= userEdit.name%>" />
                        </div>

                        <div class="input-group">
                            <label>City:</label>
                            <input type="text" name="mycity" value="<%= userEdit.city%>" />
                        </div>

                        <div>
                            <button>Save</button>
                        </div>

                    </fieldset>
                </form>
            </div>
        </div>
</body>

</html>